# ===================================================
# متطلبات مشروع المنصة التعليمية التفاعلية - Backend
# ===================================================

# إطار عمل الـ API الرئيسي
fastapi>=0.111.0

# خادم ASGI لتشغيل التطبيق (مع دعم إعادة التحميل التلقائي)
uvicorn[standard]>=0.29.0

# ORM للتعامل مع قاعدة البيانات
sqlalchemy>=2.0.0

# التحقق من صحة البيانات (مدمج مع FastAPI)
pydantic>=2.0.0

# تشفير كلمات المرور
passlib[bcrypt]>=1.7.4

# توافق bcrypt مع passlib: الإصدارات الأحدث تسبب AttributeError
bcrypt==4.0.1

# إنشاء وفك تشفير JWT (JSON Web Tokens) للمصادقة
python-jose[cryptography]>=3.3.0

# معالجة بيانات النماذج (Forms) في HTTP
python-multipart>=0.0.9

# تحديد سرعة الطلبات (Rate Limiting)
slowapi>=0.1.9
